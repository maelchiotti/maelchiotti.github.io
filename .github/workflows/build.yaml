name: Build
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build
    steps:
      - uses: actions/checkout@v2
      - id: Build
        uses: xRealNeon/VuePagesAction@1.0.1
        with:
          username: "maelchiotti"
          reponame: "maelchiotti.github.io "
          token: ${{ secrets.GITHUB_TOKEN }}
